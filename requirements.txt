# Core numeric / data stack
pandas>=2.2.0
numpy>=1.26.0

# Machine learning (for the AI optimisation layer)
scikit-learn>=1.5.0

# Backtesting framework (lightweight, no heavy GUI)
# We'll use our own engine for the core; this is a reference.
backtesting>=0.3.3

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Optional / future: brokerage SDKs
# alpaca-py>=0.30.0
# ib_insync>=0.9.86
